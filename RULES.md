# Quy tắc giao diện Slate Hasaki

## Phạm vi

Giao diện ứng dụng in tem SKU vẫn mở trực tiếp và giữ nguyên toàn bộ luồng nghiệp vụ hiện có. Phần thay đổi chỉ áp dụng cho bố cục, màu sắc và nhận diện thương hiệu.

## Object – Field – Value – Rule

| Object | Field | Field Value | Rule | Kết quả |
| --- | --- | --- | --- | --- |
| Nhận diện | Màu chủ đạo | `#005F41` | Dùng cho logo, nút chính và trạng thái nổi bật | Nhất quán thương hiệu Hasaki |
| Bố cục | Desktop | Hai cột từ 768 px | Form bên trái, danh sách chờ in bên phải | Giảm cuộn và tận dụng chiều ngang |
| Bố cục | Mobile | Một cột | Giữ nút và ô nhập dễ thao tác | Không tràn ngang ở 375 px trở lên |
| Header | Thông tin ứng dụng | Tên, logo và phiên bản | Luôn nhìn thấy ở đầu trang | Nhận diện rõ môi trường Production |
| Quản trị | Trường dữ liệu chỉ đọc | Nền slate, chữ xanh sáng | Không dùng chữ sáng trên nền trắng | Đường dẫn và mã cấu hình đọc rõ |

## Mức bằng chứng

- Giao diện và bố cục: N2 – kiểm tra trực tiếp trong trình duyệt cục bộ.
- Luồng nhập, danh sách và in: N3/N4 theo phản hồi ứng dụng và máy chủ hiện có.
- Các luồng máy in, camera và phân quyền không bị thay đổi bởi giao diện này.

## Quy tắc giao diện

- Ứng dụng không có màn hình đăng nhập; người dùng vào thẳng màn hình thao tác.
- Tên ứng dụng, logo và phiên bản phải nhìn thấy rõ ở header.
- Màu chủ đạo Hasaki `#005F41`, tương phản đủ rõ trên nền slate tối.
- Nút chính cao tối thiểu 48 px; form dùng được ở màn hình 375 px trở lên và hỗ trợ bàn phím.
- Không thay đổi dữ liệu, API, quyền in, camera, mã vạch hoặc cấu trúc tem.

## Phân quyền Quản trị

| Bề mặt | Chưa đăng nhập | `worker` | `admin` |
| --- | --- | --- | --- |
| Màn hình in tem | Được dùng | Được dùng | Được dùng |
| Màn hình Sample | Được dùng | Được dùng | Được dùng |
| Trang Quản trị | Bị yêu cầu đăng nhập | Từ chối | Cho phép |
| Đọc vai trò của chính mình | Không | Có | Có |
| Sửa bảng `user_roles` từ trình duyệt | Không | Không | Không |

- Supabase Auth xác minh email/mật khẩu và phát hành token; ứng dụng không lưu mật khẩu.
- Token chỉ lưu trong `sessionStorage`, tự mất khi đóng tab. Publishable key được phép có ở frontend; secret/service-role key tuyệt đối không đưa vào `index.html`.
- Quyền `admin` được đối chiếu từ `public.user_roles` bằng token của người dùng và RLS `auth.uid() = user_id`.
- Lệnh in đi qua Supabase RPC và không yêu cầu mã quyền Web: bất kỳ ai có link ứng dụng đều có thể gửi lệnh. Agent máy trạm vẫn bắt buộc dùng token riêng; RLS tiếp tục chặn đọc/ghi trực tiếp các bảng hàng đợi.

## Khi nào xem lại

### Tra Group UID từ database

- Nhập/quét hoặc import Group UID gọi `group_uid_lookup` theo từng lô tối đa 100 mã chính xác.
- Object là tem Group UID. SKU từ `group_uid_details.sku`; tên chỉ từ `SKU_Name.product_name`; Lot từ `batch_code`, Roll từ `roll_code`.
- Có tên sản phẩm thì tự sang Sẵn sàng in, không cần thao tác gán SKU. Không có SKU vẫn cho in khi có tên; thiếu tên thì chờ bổ sung thủ công. Không tự bịa tên từ mã hoặc tên kho.
- Mỗi UID một tem; Qty trong database là số lượng hàng, không phải số bản in. Vẫn cần bấm Xác nhận in.
- UID trùng, kể cả đang tra, không thêm lần hai. Khi đang tra không gửi in. Lỗi mạng phải báo không tra được, không khẳng định mã không tồn tại.
- RPC chỉ đọc trường phục vụ tem, không trả người cập nhật, kho, vị trí hoặc số lượng tồn. Không cấp quyền trực tiếp lên bảng. Migration `group_uid_v2_lookup.sql` đã triển khai sau khi chủ ứng dụng xác nhận quyền tra cứu không đăng nhập ngày 22/09/2026.
- Migration `group_uid_v3_drop_product.sql` loại cột tên trùng `product` theo yêu cầu; Excel WMS vẫn có thể chứa Product Name nhưng script nhập bỏ qua. SKU chưa có tên trong `SKU_Name` phải chờ bổ sung tên thủ công, không tự chuyển sang Sẵn sàng in.
- Đã kiểm tra Chromium desktop 1280px/mobile 375px bằng dữ liệu giả: tự gán, giữ lot/roll, không SKU, mã trùng đang tra, không tìm thấy, thiếu tên, lỗi mạng, gán tay và payload in. Đã kiểm tra RPC thật qua frontend: tự chuyển sang Sẵn sàng in, đủ tên/SKU/lot/roll. CHƯA VERIFY: import Excel qua trình duyệt, máy quét và bản in thật.

### Admin nạp Excel Group UID

- Chỉ tài khoản Supabase Auth có `user_roles.role = admin` được gọi RPC import. Chốt quyền phải nằm trong mỗi RPC; ẩn nút trên giao diện không phải là phân quyền.
- Frontend không có `service_role`, personal access token hoặc quyền ghi trực tiếp `group_uid_details`, `group_uid_import_runs`, `group_uid_import_rows`.
- File tối đa 10 MB / 50.000 dòng, tải staging từng lô tối đa 500. Mã trùng, thiếu Group UID, Qty âm/sai, ngày sai hoặc Status trống là lỗi chặn.
- Product Name bị bỏ qua. Ánh xạ 10 trường theo `docs/GROUP_UID_ADMIN_IMPORT_DESIGN.md`; ngày không có timezone dùng UTC+07:00.
- Phải validate đủ `uploaded_rows = expected_rows`, không có mã trùng rồi mới bật nút commit. Commit là upsert nguyên tử, không xóa mã vắng mặt và không ghi đè `Updated Date` mới hơn.
- Hash SHA-256 dùng cảnh báo file đã nạp; import lại vẫn được phép và phải idempotent. Dữ liệu staging của lượt completed/failed quá 7 ngày được dọn khi tạo lượt mới; metadata lịch sử được giữ.
- Ô **Cập nhật** mở chi tiết theo trang qua RPC `group_uid_import_update_details`; mỗi UID phải hiển thị đúng trường thay đổi, giá trị database và giá trị file. RPC chỉ đọc lượt nạp do chính Admin tạo, hỗ trợ tìm theo Group UID/SKU và không cấp cho `anon`.
- Đã kiểm tra file WMS thật 8.148 dòng trên Chromium 1280px và 375px bằng RPC mock; đã kiểm tra RPC thật bằng transaction rollback, gồm start → chunk → validate → commit. Đã xác minh `anon` không gọi được RPC và `authenticated` không ghi trực tiếp bảng. CHƯA VERIFY: thao tác đăng nhập Admin và import thật qua trang production.

Xem lại các quy tắc này khi thay đổi cấu trúc form, danh sách chờ in, header, thanh hành động, kích thước tem in, vai trò Supabase hoặc cơ chế xác minh Apps Script.

## Agent máy trạm mới

- Source agent độc lập nằm trong `workstation-agent/` và dự kiến cài tại `C:\PrintSKUAgent`.
- Agent mới hỗ trợ `sku:v1` và `group_uid:v1`; không được đọc hoặc sửa `C:\AuditFactory`.
- Quy tắc dữ liệu, bảo mật, queue lease và mức bằng chứng nằm trong `workstation-agent/RULES.md`.
- Web dùng lớp tương thích để chuyển ba thao tác hàng đợi cũ sang Supabase mà không sửa rộng bundle React đã minify.
- Màn hình `#group-uid` cho phép bỏ trống SKU; nếu nhập SKU hợp lệ thì tên sản phẩm được tra từ `public.SKU_Name`, nếu không người dùng nhập tên thủ công.
- Trang đầu `#home` hiển thị bốn lựa chọn `PRINT SKU`, `PRINT UID`, `INSPECTION` và `SAMPLE` dưới tiêu đề `WH-MATERIAL` (tiêu đề tab trình duyệt cũng là `WH-MATERIAL`).
- Màn `#inspection` (Sub-Material Inspection Report): người dùng import file PO phụ liệu (.xls BIFF8 hoặc .xlsx, đọc thuần trên trình duyệt không thư viện ngoài), app tách cột STT/SKU/Tên/Mã NCC/Số lượng rồi tách Tên phụ liệu / Trim Supplier / Màu theo quy chuẩn đặt tên SKU. Bảng cho sửa tay từng ô; SL kiểm tự tính theo bảng AQL (0.4). Xuất ra file `.xlsx` bằng cách copy template `BÁO CÁO KIỂM TRA ĐẦU VÀO PHỤ LIỆU` nhúng sẵn (base64) và chỉ ghi đè giá trị ô, giữ nguyên style/border/merge. Tách NCC/Màu: NCC lấy phần sau dấu `_` của đoạn `<mã>_<tên NCC>` (mã cho phép chứa dấu chấm/gạch, vd `W.TT.S-07-323H_Triều Vĩ` → `Triều Vĩ`); Màu lấy đoạn có chữ và cắt bỏ mã NCC ở đuôi (vd `Be ca cao-Etherea WKF-12473` → `Be ca cao-Etherea`), dự phòng theo đoạn trước `Size` hoặc đoạn dạng `Tên màu_Mã`. Cột Tên (B) giữ nguyên tên đầy đủ từ PO. Web chỉ hiển thị ba trường: P/O (sửa được), Rcvd date (sửa được) và Tổng SL (chỉ đọc). Ánh xạ ô khi xuất: C5 Buyer = MASTIGE cố định, C6 Invoice để trống, C7 Style để trống, C8 P/O (text) = số PO, C9 tổng SL, F5 Rcvd date, F6 Insp date để trống; vùng dữ liệu dòng 14–49 (A SKU, B Tên, C Supplier, D Màu, E SL hóa đơn, F Rec'd Qty = E, H SL kiểm theo AQL). Nâng cỡ chữ vùng dữ liệu lên 12pt (patch font Times New Roman index 4 & 7 trong `styles.xml` lúc xuất). Giữ nguyên độ rộng cột và toàn bộ định dạng của template — chỉ ghi giá trị vào các ô có sẵn, không được chèn ô mới (sai thứ tự cột sẽ hỏng file).
- Màn `#sample` (gom hàng mẫu vào bao): người đóng bao quét mã QR SKU trên hàng mẫu bằng máy Honeywell. SKU không có trong danh mục tổng `public."SKU_Name"` bị báo `SKU này inactive` và không mở bao. SKU hợp lệ quét lần đầu sinh bao mới rồi hiện popup chặn màn hình có nút `Xác nhận` để ghi số bao lên bao thật; quét lần 2 trở đi chỉ hiện thẻ `SKU này đã có bao` kèm số bao và số mẫu đang có, không chặn màn hình để quét liên tục.
- Mỗi SKU chiếm đúng một bao. Số bao chạy từ 1 trên **một danh sách duy nhất**, không reset theo ngày hay theo đợt — do chủ hệ thống chốt ngày 19/09/2026. Muốn chia đợt thì phải thêm cột đợt vào `sample_bags` chứ không sửa được bằng giao diện.
- Dữ liệu bao nằm ở bảng `public.sample_bags` (`bag_no`, `sku`, `quantity`), tạo bằng `supabase/sample_bag_v1.sql`. Bảng bật RLS và bị thu hồi quyền với `anon`/`authenticated`; web chỉ đi qua bốn RPC security definer `sample_scan`, `sample_list`, `sample_decrement`, `sample_delete` — cùng khuôn với hàng đợi in.
- `sample_scan` khoá bằng `pg_advisory_xact_lock` trước khi cấp số bao mới nên hai máy quét cùng lúc không giành trùng số. Số bao mới lấy `max(bag_no) + 1`: xoá bao cuối rồi quét lại thì số đó được dùng lại, còn xoá bao giữa thì để lại khoảng trống — cố ý, vì số bao đã ghi lên bao thật không được đánh lại.
- Chốt chặn SKU inactive nằm ở **cả hai đầu**: giao diện hiển thị thông báo, còn `sample_scan` từ chối ghi. Không bỏ chốt phía RPC — nó là thứ duy nhất giữ cho bảng bao không dính SKU lạ.
- Sửa quét nhầm ngay trên danh sách chỉ còn **một** thao tác: nút `−` bớt 1 mẫu (bao còn đúng 1 mẫu thì biến mất luôn vì bao rỗng vô nghĩa). Mỗi lần bấm tối đa hỏng 1 mẫu.
- **Xoá cả bao đã bị gỡ khỏi giao diện và thu hồi quyền dưới database** (`supabase/sample_bag_v2_lock_delete.sql`). Lý do: app không có màn hình đăng nhập, nên bất kỳ ai có link cũng gọi được mọi RPC đang mở cho `anon` — bỏ nút thôi là vô nghĩa, phải `revoke execute` mới thật sự chặn. Đã kiểm chứng: gọi thẳng `sample_delete` bằng publishable key trả `42501 permission denied for function sample_delete`. Hàm vẫn còn đó cho người quản trị chạy qua SQL Editor hoặc `scripts/apply_supabase_sql.py`.
- Hệ quả phải chấp nhận: huỷ một bao đang có 20 mẫu phải bấm `−` 20 lần. Đó là chủ ý — thao tác phá dữ liệu phải chậm và có giới hạn thiệt hại mỗi lần bấm.
- Bảng bao không lưu tên sản phẩm; tên luôn tra lại từ `public."SKU_Name"` để danh sách và file xuất không mang tên cũ.
- `Xuất Excel theo dõi` dựng file `.xlsx` từ đầu (không template) bằng bộ ghi zip `window.PrintSkuZip.buildZip` dùng chung với màn Inspection: cột `STT bao / SKU / Tên sản phẩm / Số lượng / Mở bao lúc / Cập nhật lúc`, dòng cuối là `TỔNG`.
- **Cách chỉnh trên máy Honeywell, đã xác minh ngày 19/09:** `Settings → Honeywell Settings → Scanning → Internal Scanner → Default Profile → Data Processing Settings → Wedge Method`, đổi từ **Standard** sang **Keyboard**. `Standard` bơm phím giả ở tầng hệ thống — ô nhập gốc Android nhận được nhưng nội dung web trong Chrome thì không. `Keyboard` gõ qua IME Honeywell nên trang web nhận được như gõ tay. Đây là cách duy nhất làm máy quét chạy với app, không phải việc của code.
- Ô SKU mặc định đặt `inputmode="none"` để bàn phím ảo Android không bật lên che màn hình. Nhưng máy quét chế độ Keyboard gõ qua chính IME đó, nên **bắt buộc phải có nút bật lại** — trạng thái nhớ trong `localStorage` của từng máy. Đừng biến nó thành hằng số: nếu giấu bàn phím làm mất luôn đường nhập thì người vận hành kẹt cứng.
- Khung `Danh sách bao` dạng thả: bấm đầu khung mở ra, bấm lần nữa thu gọn; trạng thái nhớ trong `localStorage`, mặc định thu gọn cho gọn màn hình máy cầm tay. Badge đếm số bao, tổng mẫu và nút xuất Excel **luôn nhìn thấy** kể cả khi đang thu gọn.
- **Không được chờ đúng phím Enter.** Mỗi máy quét cấu hình một kiểu: có máy gửi Enter ở cuối, có máy không, có máy nhét cả cụm qua IME Android mà không sinh phím nào. Ô SKU tự chốt khi đứng yên 320 ms sau một cụm nhập nhanh (khoảng cách giữa hai ký tự dưới 60 ms, hoặc cả cụm vào một lúc) và dài từ 5 ký tự. Gõ tay chậm thì không tự chốt — phải Enter hoặc bấm nút, để không chốt hụt giữa chừng khi người ta đang gõ dở.
- `cleanScan` cắt tiền tố AIM identifier (`]Q0`, `]C1`…) mà nhiều máy quét bật sẵn, và lọc ký tự điều khiển / vô hình lọt vào theo cấu hình suffix. Hàm này **cố ý không dùng escape regex nào** (`\uXXXX`, `\]`) — viết bằng so sánh chuỗi và mã số ký tự — vì escape bị biến dạng khi đi qua lớp công cụ và đã một lần làm cả màn Sample chết vì regex hỏng.
- `scanner-check.html` là trang chẩn đoán máy quét, mở trực tiếp cạnh `index.html`. Nó hiện chuỗi thô, số ký tự, ký tự kết thúc, nhật ký từng phím, và tự tra `SKU_Name` để phân biệt ba ca: máy quét không gửi gì, gửi thiếu Enter, hay mã QR chứa nhiều thứ ngoài SKU. Trang này chỉ đọc, không ghi vào bảng bao.
- **Máy Honeywell ngoài hiện trường (đo 19/09) KHÔNG đưa được ký tự vào trình duyệt — lỗi nằm ở cấu hình máy, không phải ở app.** Đo hai lần bằng `scanner-check.html`: mỗi lần quét sinh 4–5 sự kiện `keydown` mang `key="Unidentified"`, `keyCode = 0`, và **không có một sự kiện chữ nào** (`beforeinput`, `input`, `compositionstart/update/end`, `paste` đều trống), ô nhập rỗng. Số phím (4, rồi 5) không khớp độ dài SKU (9 ký tự) nên đó không phải mã đang được gõ. Kết luận: máy đang chạy chế độ Intent/broadcast chứ không phải Keyboard Wedge, hoặc dùng kiểu inject phím mà nội dung web không dịch ra ký tự. **Không có thay đổi nào trong trang web lấy được mã ở chế độ đó** — đừng mất công sửa app, phải đổi cấu hình trên máy.
- **Máy quét đi qua IME Android, không phải bàn phím HID thuần.** Đo thật ngày 19/09 bằng `scanner-check.html`: một lần quét sinh 5 sự kiện phím cách nhau ~50 ms nhưng **mọi phím đều mang `key="Unidentified"`, `keyCode = 0`** — ký tự thật không nằm trong sự kiện phím, nó chỉ chui vào ô đang được focus. Hệ quả: lọc phím theo `key.length === 1` là bỏ sót đúng loại máy này, và focus rơi ra ngoài ô là mất nguyên mã, không cứu lại được. Vì vậy màn `#sample` vừa bắt cả `Unidentified`/`Process`, vừa kéo con trỏ về ô mỗi khi nó rơi ra ngoài (trừ lúc người dùng đang bấm nút trong màn hoặc popup đang mở).
- **Timeout phía client không chứng minh được là server chưa ghi.** Đo thật ngày 19/09: một lệnh xoá bao báo `signal timed out` nhưng đã xoá xong trên database. Vì vậy thông báo lỗi tuyệt đối không được nói "chưa ghi gì" — nó phải nói là chưa rõ, và tự tải lại danh sách để người dùng nhìn sự thật từ server. Nói chắc ở đây thì người quét sẽ quét lại và bao bị đếm gấp đôi.
- Máy quét Honeywell gõ như bàn phím nên mất focus là mất nguyên mã: màn `#sample` kéo mọi phím ký tự lạc về ô SKU. Lúc popup bao mới đang mở thì ô SKU bị chặn — phải bấm `Xác nhận` (hoặc `Esc`) rồi mới quét tiếp, nên mã quét trong lúc popup mở sẽ mất.
- **Ô tên hàng ở `PRINT SKU` phải bị đặt lại mỗi khi ô SKU đổi giá trị**, và phải bị xoá khi tra cứu danh mục thất bại. Ngày 23/09/2026 đã in ra tem mang **số của mã này nhưng tên của mã khác**: tên của `[SKU_DA_XOA]` (`…/38/`) nằm trên tem số `[SKU_DA_XOA]`, tên của `[SKU_DA_XOA]` (`…/30/`) nằm trên tem số `[SKU_DA_XOA]`. Người vận hành nhập đúng; lỗi do mã cũ chỉ điền tên khi ô tên đang rỗng (`ae&&!e.trim()`) nên sửa số mà tên ở nguyên, còn nhánh tra cứu lỗi mạng (`S("mang")`) thì thoát mà không đụng tới tên. Tệ hơn, `pl(Y,j)` ghi cặp sai đó vào `localStorage` `tem-sotay`, khiến máy đó lặp lại lỗi mãi mà không cần mạng. Bộ nhớ `tem-sotay` chỉ còn là gợi ý hiện nhanh, **không bao giờ đi vào tem**: tên nào chưa được danh mục xác nhận cho đúng SKU đó thì bị chặn không cho thêm vào danh sách in. Nhờ vậy bộ nhớ có nhiễm bẩn cũng vô hại và không phải xoá tay.

Bốn quy tắc bắt buộc giữ, tương ứng bốn đường vào:
  - **gõ tay** đổi ô SKU → đặt lại tên theo đúng SKU mới (tên nhớ của chính nó hoặc rỗng), đánh dấu chưa xác nhận
  - **quét mã** → y như trên, tuyệt đối không giữ tên của mã trước
  - **danh mục trả về tên** → ghi tên và đánh dấu đã xác nhận
  - **tra cứu lỗi mạng, hoặc mã không có trong danh mục** → xoá tên, bắt nhập tay có chủ đích
Người dùng tự gõ tên thì luôn được phép in — đó là chủ ý, vì mã mới chưa lên danh mục vẫn phải in được.
- Màn hình in tem SKU (`#worker`) mang tiêu đề `PRINT SKU` kèm dòng phiên bản; nút `← WH-MATERIAL` ghim ở mép trên bên phải (desktop trong dải header, mobile thành nút tròn `←` ngay dưới header). Ba dòng hướng dẫn tĩnh dưới ô nhập đã bỏ; dòng nhắc động chỉ hiện khi có nội dung (đang tra/kết quả tra SKU). Font toàn app thống nhất Arial.
- Màn `#group-uid` dùng thanh header dính giống `PRINT SKU`: logo máy in + tên `PRINT UID` + dòng phiên bản, pill trạng thái máy in (tự cập nhật 20 giây/lần từ hàng đợi: `Máy in rảnh` / thông báo lỗi máy in / `Chưa rõ máy in` khi agent mất liên lạc quá 15 giây) và nút `← WH-MATERIAL`.
- `PRINT UID` chia đúng 4 cụm đánh số: `1 Thêm Group UID` và `3 Chưa gán SKU` ở cột trái; `2 Gán SKU` và `4 Sẵn sàng in` ở cột phải (mobile xếp dọc 1 → 3 → 2 → 4 theo trình tự thao tác).
- Cụm 1 nhận UID theo hai cách: quét/nhập từng mã (Enter thêm liên tục) hoặc `Import từ file Excel` — đọc file export Group UID từ WMS (.xlsx), tự tìm cột `Group UID Code`, bỏ qua UID trùng và UID không hợp lệ; parser dùng DecompressionStream sẵn có của Chromium, không thêm thư viện ngoài.
- Cụm 2 có hai chế độ gán nằm trong hai tab riêng (`Gán theo tick chọn` mặc định, `Gán tự động`); mỗi lúc chỉ hiện một tab. Gán theo tick chọn: tick UID ở cụm 3 (hoặc `Chọn tất cả`) rồi nhập SKU (bỏ trống được, hiện nhãn `Không SKU`) và tên sản phẩm; Enter trong ô SKU/tên sản phẩm cũng kích hoạt gán. Gán tự động: nhập nhiều dòng `SKU + số UID + Tên SP` (mỗi dòng 1–500), hệ thống chia UID theo thứ tự danh sách cụm 3 từ trên xuống; tổng số UID các dòng không được vượt số UID đang chờ. Tên SP tự tra từ `public.SKU_Name` khi gõ SKU và có thể sửa/nhập tay; SKU được bỏ trống nếu đã có Tên SP (ô Tên SP gợi ý sẵn hai tên kho `WH - MATERIAL - GARMENT` và `WH - MATERIAL - MTG`); dòng nào thiếu cả SKU lẫn Tên SP, hoặc có SKU nhưng tra không được và không nhập tay, thì báo lỗi và không gán dòng nào.
- Tab `Gán theo tick chọn` có thêm hai ô `Lot` và `Roll` (tối đa 20 ký tự, được bỏ trống). Giá trị gán theo từng UID và in ở đáy tem, thay cho việc ghi bút. Khung 4 hiện kèm `Lot …` / `Roll …` để soát trước khi in. Tab `Gán tự động` không có hai ô này.
- SKU có thể bỏ trống ở chế độ tick chọn. Tên sản phẩm và Group UID luôn bắt buộc; mỗi Group UID cố định in đúng 1 tem.
- Trong cụm 4 `Sẵn sàng in`, mỗi dòng có nút bỏ gán (trả UID về khung chờ, xóa mapping) và nút xóa hẳn. Nút `Xác nhận in` chỉ gửi các UID trong khung sẵn sàng; khung chờ giữ nguyên.
- Một lần xác nhận trên giao diện gửi toàn bộ danh sách thành một lệnh `group_uid:v1` dạng batch (`payload.items`, tối đa 100 UID mỗi lệnh; vượt quá thì tự chia thành nhiều lệnh).
- Agent từ 0.3.0 trải phẳng mọi tem của một lệnh rồi ghép 2 tem liền kề — kể cả 2 Group UID khác nhau — lên cùng một hàng giấy 2 tem, nên không còn phí tem bên phải; chỉ tem cuối cùng của lệnh có tổng lẻ mới để trống nửa hàng. Agent 0.2.2 trở xuống không đọc được lệnh batch và phải được cài lại.
- Muốn sửa mapping của một UID đã gán: bấm bỏ gán để trả về khung chờ rồi gán lại; không chỉnh riêng trên từng dòng.
- Khung 3 và 4 hiển thị dạng danh sách gọn, có badge đếm số dòng; nội dung dài rút gọn bằng dấu ba chấm nhưng vẫn có tooltip xem đầy đủ. Danh sách dài cuộn bên trong khung, khu vực gán và nút in luôn nhìn thấy.

## Làm mới SKU từ Mastige Inside

Nguồn dữ liệu `SKU_Name` là màn `Products` trên `inside.mastige.vn`. Có hai đường, dùng cho hai mục đích khác nhau.

**Đường thường dùng — đọc thẳng bảng danh sách, không qua Excel:**

```
python scripts/refresh_skus.py quick     # in đoạn JS, dán vào Console tab inside đã đăng nhập
python scripts/refresh_skus.py apply     # nạp sku-changes-*.json vào data/sku.db
python scripts/refresh_skus.py sync      # upsert lên Supabase (không xoá dòng nào)
```

`apply` ghi nhận file JSON đã nạp nhưng giữ file trong Downloads. `sync` chỉ dọn đúng các file JSON đã nạp sau khi **toàn bộ lô upsert Supabase thành công**; file mới/chưa nạp hoặc đã bị sửa nội dung được giữ lại. `sync --dry-run` và lần sync lỗi không xoá file. Nếu `apply` dùng `--downloads` khác mặc định, truyền cùng thư mục đó cho `sync --downloads`. Không dùng `apply --cleanup` vì xoá trước khi đồng bộ có thể làm mất dữ liệu cần thử lại.

**Đường đối chiếu — tải Excel toàn bộ, chạy khi cần kiểm chứng:**

```
python scripts/refresh_skus.py snippet
python scripts/refresh_skus.py merge --cleanup
python scripts/refresh_skus.py sync
```

- Trang danh sách sắp theo `Modified` giảm dần và nhận tham số `&limit=` (mặc định 100, chạy tốt tới 3.000), nên `quick` chỉ đọc từ trên xuống tới khi chạm dòng cũ hơn mốc cắt rồi dừng. Đo thật ngày 18/09: **21 giây, 361 dòng, file JSON 112 KB** — so với ~10 phút và 5 MB Excel.
- Mốc cắt lấy theo `--days` (mặc định 7) chứ không lưu trạng thái lần chạy trước: nạp lại dòng không đổi là vô hại, còn mất trạng thái thì không bao giờ gây sót.
- Đổi tên SKU **có** làm `Modified` nhảy — đã kiểm chứng với 3 SKU đổi tên ngày 17/09, cả ba đều mang mốc `26-09-17 10:58`.

**Hai giả định do chủ hệ thống chốt ngày 18/09/2026:**

1. Inside **sẽ không đổi cấu trúc** bảng danh sách.
2. `Modified` **sẽ không bỏ sót** bất kỳ loại thay đổi nào.

Vì vậy đường Excel không cần chạy định kỳ nữa, chỉ chạy **khi được yêu cầu đối chiếu**. Hai chốt chặn trong `apply` vẫn giữ nguyên vì chúng không tốn gì khi giả định còn đúng — cầu dao đổi tên chưa bao giờ nổ trong vận hành bình thường, và chốt chặn chống ghi rỗng cũng vậy. Chúng chỉ có tác dụng đúng vào ngày một trong hai giả định trên sai, và khi đó biến sự cố âm thầm thành một lần dừng ồn ào.

Nếu về sau `apply` báo dừng vì đổi tên hàng loạt, hoặc một đợt đối chiếu Excel cho ra chênh lệch, thì chính là một trong hai giả định trên đã hết đúng — xem lại từ đó.

- Danh sách category nằm ở `scripts/sku_categories.json`. Thêm/bớt category chỉ sửa file này, không sửa code. `id` phải khớp `category_id` trên Inside, `name` phải khớp tên đang dùng trong Supabase.
- `merge` chỉ nhận file export chứa **đúng một** category và tự đối chiếu `category_id` bên trong file với cấu hình — không bao giờ nạp file dưới nhãn category sai. File nhiều category bị bỏ qua và liệt kê trong `ignored_files`.
- `merge` vừa thêm SKU mới vừa **cập nhật SKU đã có** khi tên, trạng thái, category, barcode, thương hiệu hoặc giá khác với file export; báo cáo có `updated` và `changed_fields` cho biết cột nào đổi bao nhiêu dòng. `--no-update` giữ hành vi chỉ thêm như bản đầu. Không có bước này thì tên sản phẩm đổi trên Inside sẽ không bao giờ tới được Supabase và tem in ra mang tên cũ.
- Cột số (`price`, `latest_cost`, `product_average_cost`) so theo **giá trị số** chứ không so chuỗi: bản nạp cũ lưu chuỗi rỗng còn export ghi `0`, so thô sẽ báo "đã đổi" cho gần như mọi dòng (đo thật: 7.752 dòng báo đổi trong khi chỉ 84 dòng đổi thật).
- Category nào chưa có file sẽ nằm trong `missing` và lệnh thoát khác 0; chạy lại snippet cho những category đó.
- Chỉ xét file tải trong 180 phút gần nhất (đổi bằng `--since-minutes`) để không nạp nhầm file export cũ. Khi có nhiều file cùng category thì lấy file mới nhất.
- Sao lưu trước khi nạp bằng `merge --backup data/sku.before-refresh.db` (chỉ sao lưu một lần cho cả lượt chạy).
- `merge --cleanup` xoá file export sau khi nạp xong. Chỉ xoá file của category đã nạp thành công, kể cả các bản trùng; file không đọc được, file nhiều category, file của category còn thiếu và mọi file khác trong thư mục đều giữ nguyên. Mặc định tắt vì đây là xoá file trong thư mục Downloads của người dùng.
- Mỗi lần bấm Download, Inside kích hoạt tải **hai lần** nên một category thường để lại 2 file trùng; `merge` luôn lấy file mới nhất nên không ảnh hưởng kết quả, nhưng không dọn thì Downloads phình dần.

Ràng buộc của Inside — đã kiểm chứng, đừng mất công thử lại:

- Không gọi được endpoint export từ ngoài: request trực tiếp trả **401**, nó chỉ chạy khi được nạp như script con từ trang inside đã đăng nhập. Vì vậy bắt buộc phải qua trình duyệt, không viết được script Python thuần.
- URL export dựng từ **địa chỉ trang**, không phải ô select category; muốn đổi category phải đổi trang (snippet dùng iframe cùng origin nên không phải tải lại tab đang mở).
- Server **cache file export theo từng category mỗi ngày** nên export lại gần như tức thì — retry không tốn gì.
- HTTP **503 khi tải nghĩa là file chưa tạo xong**, không phải lỗi quyền; gọi lại là được. Snippet tự thử tối đa 3 lần bằng cách đọc nội dung iframe.
- Export không lọc category (toàn bộ catalog) chạy quá 8 phút chưa xong — không dùng.
- **Tên sản phẩm trong bảng HTML nằm trong thẻ `<a>` của ô Product Name; chữ đứng trước thẻ đó là tên thương hiệu và KHÔNG thuộc `product_name`.** Ô đó là `No brand - <a>Chỉ quấn chân nút/...</a>`. Lấy `textContent` cả ô sẽ dính tiền tố và làm đổi sai toàn bộ 21.000 tên; phải lấy đúng nội dung thẻ `<a>`.
- **Bảng HTML render RỖNG các cột Barcode, LatestCost, Average Cost** — giá chỉ có trong file Excel. Vì vậy `apply` chỉ được phép cập nhật `product_name`, `status`, `category_id`, `category_name`. Lúc chạy thử đã đưa cả cột giá vào và ghi rỗng đè mất giá vốn của 44 dòng. Ngoài việc giới hạn danh sách cột, `apply` còn có chốt chặn thứ hai: không bao giờ ghi giá trị rỗng đè lên dữ liệu cũ.
- **Cầu dao đổi tên hàng loạt.** `apply` huỷ cả lượt nạp nếu số dòng đổi tên vượt `max(25 dòng, 20% số dòng đối chiếu)`. Lý do: lỗi đọc dữ liệu luôn làm gần như MỌI dòng "đổi tên", còn người thật sửa tên chỉ vài chục dòng. Đã thử lại bằng cách giả lập đúng lỗi tiền tố thương hiệu trên 300 dòng — cầu dao chặn và rollback. Chỉ dùng `--force` sau khi đã nhìn tận mắt danh sách tên mới.
- Kết quả kiểm chứng ngày 18/09 trên 1.666 dòng thuộc cả 7 category: tên đọc từ HTML **khớp tuyệt đối 1.666/1.666** với tên lấy từ Excel. Không có dòng nào bị cắt ngắn (dài nhất 191 ký tự), không có HTML entity, mỗi ô đúng một thẻ `<a>`. Phân trang cũng sạch: `page=1&limit=200` cho dòng 0–199, `page=2&limit=200` cho dòng 200–399, không trùng không sót.
- Nút Download thật là `#download-products`; trong trang còn link `Products` khác ở breadcrumb, click nhầm sẽ không kích hoạt export.

### Hạn chế còn lại: SKU ngừng Active

Export luôn lọc `status=1`, nên SKU bị ngừng Active **biến mất khỏi file** thay vì xuất hiện với trạng thái mới. Hệ quả: `merge` không thấy nó để cập nhật, và `sync` (chỉ gửi dòng `status = '1'`, upsert theo `sku`, không xoá) cũng không đụng tới bản ghi cũ trên Supabase. Bản ghi đó nằm lại vĩnh viễn với trạng thái Active.

Hiện tại đã có 3 SKU như vậy ở category 954 (`[SKU_DA_XOA]`, `[SKU_DA_XOA]`, `[SKU_DA_XOA]`) — Supabase 21.161 dòng so với 21.158 dòng local.

Chưa xử lý tự động vì phải quyết định trước: xoá hẳn khỏi Supabase thì tem đã in không tra được tên nữa, nên hướng đúng là đánh dấu ngừng Active chứ không xoá. Muốn làm thì cần đối chiếu danh sách SKU của từng category giữa local và export rồi đẩy trạng thái mới lên, và phải có chốt chặn không cho một file export lỗi/thiếu dòng làm ngừng hàng loạt SKU.


## Fabric Relaxation (23/09/2026)

- Nguồn yêu cầu: ảnh tem xả vải và chỉ đạo dùng cùng giấy/agent SKU, UID.
- Object: lệnh in `fabric_relaxation:v1`; mã hàng nhập tay, chưa đối chiếu danh mục (N3).
- Mã hàng: 1–40 ký tự chữ/số/chấm/gạch ngang/gạch dưới; giữ số 0 đầu. Rỗng hoặc ký tự khác: NG, chặn in.
- Số tem: số nguyên 1–500; ngoài phạm vi: NG. Giới hạn kế thừa hàng đợi hiện có.
- Tem 40 × 60 mm; 2 cột, khe ngang 2 mm, khe hàng 3 mm, 8 dot/mm, cùng renderer TSPL SKU/UID.
- Ngày, Giờ, Lot luôn là dòng chấm trống; không điền thời gian hệ thống hoặc dữ liệu Lot.
- Mã dài chia dòng mỗi 16 ký tự, tối đa 3 dòng; font monospace để không cắt mã rộng.
- Gửi qua PrintSkuQueue; khi lỗi mạng giữ nonce cho cùng dữ liệu để thử lại không tạo job trùng.
- Agent cũ không claim Fabric; cần migration v2 và agent 0.5.0. Giữ nguyên cơ chế quyền, lease và spooler hiện có.
- CHƯA VERIFY: chất lượng trên máy in vật lý, căn tem và độ đậm với cuộn giấy thực tế. Xem lại khi đổi giấy, driver, máy in hoặc quy tắc mã hàng.

### Fabric Relaxation v2 — nhiều mã trên một tem

- Object: lệnh in `fabric_relaxation:v2`; `itemCodes` là mảng 1–5 mã nhập riêng trên frontend (N3).
- Mỗi mã nằm đúng một dòng; font tự giảm để mã tối đa 40 ký tự không xuống dòng.
- Theo ảnh mẫu được người dùng đính chính: mã xếp liên tiếp phía trên; chỉ một bộ Ngày/Giờ/Lot dùng chung phía dưới, mỗi mục một dòng trống dài để ghi tay.
- Agent 0.6.1 sửa bố cục v2, hỗ trợ cả v1 và v2; không đổi payload hoặc migration. Máy trạm cần nâng 0.6.1 để in đúng mẫu mới.
- Migration áp dụng: `print_queue_v3_fabric_relaxation_multi_item.sql`; có thể chạy trực tiếp sau schema v1 hoặc v2.
- CHƯA VERIFY: khả năng ghi tay trên năm hàng ở tem thật 40 × 60 mm; phải in thử 1 và 5 mã trước khi dùng sản xuất.

### Fabric Relaxation v3 — tem viết tay hoàn toàn

- Frontend chỉ nhận số lượng 1–500; không còn ô nhập Mã hàng.
- Payload cố định `{handwritten:true}` với `templateVersion:3`; agent capability là `fabric_relaxation:v3`.
- Tem in: Mã hàng ở đầu tem và chừa khoảng hai dòng để viết; Lot nằm bên dưới; Ngày có mẫu `..... / .....`; Giờ có mẫu `..... : .....`.
- Cỡ chữ Mã hàng, Lot, Ngày và Giờ là 28, tăng 2 so với bố cục v2.
- Agent 0.7.0 vẫn giữ khả năng in job Fabric v1/v2 đang chờ. Migration áp dụng: `print_queue_v4_fabric_relaxation_handwritten.sql`.
- CHƯA VERIFY: khoảng viết tay và độ rõ cỡ chữ trên tem thật 40 × 60 mm; in thử trước khi dùng sản xuất.

## Cắt Group UID (23/09/2026)

- Object: một bản ghi `cut_group_uids`, khóa duy nhất là `group_uid_code`. Field snapshot gồm SKU, UID, Lot, Roll, Tên SP, thời gian cắt, trạng thái in và mã job in.
- Nguồn Field Value: `group_uid_details` cung cấp UID/SKU/Lot/Roll; `SKU_Name` cung cấp Tên SP. Dữ liệu được chụp lại lúc cắt để lịch sử không đổi theo danh mục về sau (N4: đối chiếu database nội bộ).
- Rule OK: UID dài 1–40 ký tự, tồn tại trong Group UID, có SKU và có Tên SP. Lot/Roll được phép rỗng vì mẫu tem hiện tại đã hỗ trợ trường hợp này.
- Rule NG: UID không tồn tại hoặc thiếu SKU/Tên SP thì chặn lưu và yêu cầu cập nhật dữ liệu Group UID. UID đã có trong bảng cắt bị chặn, kể cả tem cũ đã in, để tránh cắt hai lần.
- Trạng thái: `pending` (chờ in), `queued` (đã gửi agent), `printed` (agent hoàn tất), `failed` (agent báo lỗi). Chỉ `pending`/`failed` được chọn in.
- Một job tối đa 100 UID theo giới hạn batch `group_uid:v1`; mỗi UID đúng 1 tem và dùng nguyên format Group UID hiện tại.
- Tra cứu theo SKU dùng so khớp chứa, tối đa 5.000 dòng/lượt. Excel chỉ xuất đúng kết quả đang lọc với 5 cột SKU, UID, Lot, Roll, Tên SP; worksheet dùng Arial 12, header xanh dương, đóng khung từng ô và tự xuống dòng. Khổ A4 ngang chỉ co vừa một trang theo chiều rộng; nhiều UID tiếp tục sang trang 2, 3… và lặp lại header trên mỗi trang.
- Backend bật RLS và thu hồi quyền bảng trực tiếp; frontend anon chỉ gọi các RPC `cut_group_uid_*`, theo mô hình truy cập hiện hữu của ứng dụng.
- Ô quét (`#cut-code`, 25/09/2026) dùng đúng cơ chế đã chứng minh ở màn `#sample`: `inputmode="none"` ẩn bàn phím ảo Android khi quét, kèm nút bật lại nhớ theo từng máy (`localStorage` khóa `cut-ban-phim`) vì máy quét chế độ Keyboard đi qua chính IME đó nên đôi khi cần bàn phím đang bật mới gõ được. Listener `keydown` toàn cục tự kéo focus về ô quét bất cứ khi nào rơi ra ngoài (trừ lúc đang gõ ở ô khác như lọc SKU, hoặc đang bấm nút/checkbox thật trong màn) — máy quét kiểu IME (Honeywell…) chỉ bơm ký tự vào đúng ô đang focus, rơi ra ngoài là mất nguyên mã không cứu lại được, nên người vận hành không cần bấm tay vào ô trước khi quét. Cơ chế này CHỈ giữ focus, không tự submit: lưu tem vẫn đúng như cũ, chỉ xảy ra khi form submit (Enter trong ô hoặc bấm nút "Lưu tem").
- CHƯA VERIFY: bản in vật lý sau khi đi qua máy trạm và cách Excel phân trang trên phiên bản Microsoft Excel tại kho. Xem lại sau lần in/xuất thật đầu tiên.
- **Bố cục lại + gấp danh sách tem chờ in (26/09/2026).** Đổi thứ tự 3 khung: "Tra cứu UID đã cắt" lên số 2 (cột phải, cạnh "Quét Group UID"), "Tem chờ in" xuống số 3 (hàng dưới, full-width). Khung "Tem chờ in" mặc định GẤP LẠI — chỉ hiện huy hiệu đếm số tem (vẫn cập nhật realtime dù đang gấp), chạm tiêu đề mới mở ra để thấy danh sách + tick chọn + nút in, nhớ trạng thái mở/gấp theo `localStorage` (`cut-mo-tem-cho-in`) giống hệt cơ chế "Danh sách bao" ở `#sample`. Lý do: tránh hiện sẵn cả danh sách hàng trăm tem ngay trước mắt gây rối mắt/dễ bấm nhầm — chỉ mở ra khi người dùng thật sự muốn chọn tem để in.
- **Chọn tem in ngay từ bảng Tra cứu, lọc thêm theo Lot (26/09/2026).** `cut_group_uid_search` thêm tham số `p_lot` (đổi chữ ký hàm nên phải `drop function` cũ rồi tạo lại, xem `supabase/cut_group_uid_v3_search_lot_filter.sql`) — bảng Tra cứu giờ lọc được cả theo SKU và theo Lot. Mỗi dòng còn tồn tại để chọn in (`pending`/`failed`) có thêm checkbox ở cột đầu; nút "Đưa vào hàng chờ in" lấy các dòng đã tick, cộng dồn vào một `Set` mã UID ở client (`stagedCodes`, KHÔNG phải cột/status mới trong database — chỉ là trạng thái "đã chọn để in" tạm thời trên trình duyệt), rồi tự mở khung "Tem chờ in" và tick sẵn đúng các UID đó trong danh sách — người dùng bấm "In tem đã chọn" ở đó như cũ, không đổi cơ chế in/enqueue. Bảng Tra cứu và danh sách "Tem chờ in" dùng chung `stagedCodes` nên tick ở đâu cũng đồng bộ hai bên (bỏ tick ở danh sách "Tem chờ in" cũng gỡ khỏi `stagedCodes`). Lý do: danh sách "Tem chờ in" không lọc được, khó tìm đúng lô cần in giữa hàng trăm tem; bảng Tra cứu đã có sẵn lọc SKU/Lot nên chọn ở đó rồi "đẩy" qua sẽ nhanh hơn.
- **Tem chờ in chỉ hiện đúng phần đã chọn, không hiện cả tồn kho pending (26/09/2026, bản 2).** `renderOpen()` lọc `openItems` theo `stagedCodes` trước khi vẽ — huy hiệu số lượng và danh sách trong khung "Tem chờ in" giờ chỉ phản ánh đúng số tem người dùng vừa chọn/đẩy qua từ bảng Tra cứu (vd chọn 3 thì hiện đúng 3, không phải toàn bộ hàng trăm tem `pending` đang tồn trong hệ thống). `openItems` (danh sách gốc từ `cut_group_uid_list`) vẫn tải đầy đủ như cũ — chỉ MÀN HÌNH hiển thị bị lọc lại theo lựa chọn, dữ liệu polling/queue phía dưới không đổi.
- **Bố trí lại thanh hành động của bảng Tra cứu (26/09/2026, bản 3).** "Xuất Excel A4 ngang" chuyển từ nằm trong form lọc (`#cut-search`) xuống cùng hàng với "Chọn tất cả tem có thể in trong bảng" và "Đưa vào hàng chờ in" ở dưới bảng — form lọc giờ chỉ còn ô SKU/Lot + nút Tìm kiếm, không lẫn với nút hành động trên kết quả. Đổi luôn `cut-export` sang kiểu `cut-secondary` (viền, không tô xanh) để "Đưa vào hàng chờ in" nổi bật là hành động chính, giống cách "Tải lại"/"In tem đã chọn" phân cấp ở khung Tem chờ in. "Chọn tất cả tem có thể in trong bảng" chỉ tick các dòng ĐANG hiện trên bảng (tức đang theo đúng bộ lọc SKU/Lot hiện tại) — lọc trước rồi chọn tất cả là bấm 1 lần thay vì tick từng UID.
- **Chuyển 3 khung thành 3 cụm dạng tab: Scan/Data/Print (26/09/2026, bản 4).** Bỏ layout 3 khung hiện cùng lúc — giờ chỉ 1 cụm hiện nội dung tại một thời điểm, chạm vào thanh tab (`#cut-tab-scan`/`#cut-tab-data`/`#cut-tab-print`) để chuyển, nhớ cụm đang mở theo `localStorage` (`cut-cum-dang-mo`). Huy hiệu số lượng (`#cut-found`, `#cut-count`) nằm ngay trên thanh tab nên vẫn thấy được dù đang ở cụm khác. Thông báo trạng thái (`#cut-message`, hàm `tell()`) chuyển ra NẰM NGOÀI 3 panel, ngay dưới thanh tab — vì trước đó nó nằm trong panel Scan nên khi đứng ở tab Data/Print bấm tìm kiếm/in sẽ không thấy phản hồi gì (panel Scan bị ẩn). Mỗi cụm có nút nhảy sang cụm khác: Scan có "Xem trong Tra cứu →", Print có "← Chọn thêm ở Tra cứu"; bấm "Đưa vào hàng chờ in" ở Data cũng tự chuyển sang tab Print luôn (`setActiveTab('print')`) thay vì tự mở/gấp khung như bản trước. Bỏ hẳn cơ chế gấp/mở lồng bên trong cụm Print (`cut-queue-toggle`/`cut-queue-body`/`QUEUE_STORE`) vì tab đã tự nhiên giải quyết đúng yêu cầu "không hiển thị sẵn, bấm mới thấy". Máy quét IME giữ focus ở ô quét (`document.addEventListener('keydown',...)`) giờ CHỈ áp dụng khi đang ở tab Scan (`activeTab==='scan'`) — trước đó áp dụng toàn màn hình bất kể tab nào đang mở, có thể cướp focus của người dùng khi họ đang gõ lọc SKU/Lot hoặc tick chọn ở tab Data/Print (dù thực tế it xảy ra vì `typingElsewhere()` đã chặn khi đang focus vào input/checkbox, đây là lớp bảo vệ thêm cho đúng ý định).
- **Cho phép chọn IN LẠI UID đã "Đã in" (26/09/2026, bản 4).** `cut_group_uid_mark_queued` nới điều kiện từ `print_status in ('pending','failed')` thành thêm cả `'printed'` (xem `supabase/cut_group_uid_v4_allow_reprint.sql`, không đổi chữ ký hàm nên chỉ `create or replace`, không cần `drop`). Lý do: tem vật lý có thể hư/mất/lem cần in lại dù đã in xong trước đó — trước đây bảng Tra cứu KHÔNG hiện checkbox cho dòng "Đã in" nên không có cách nào chọn lại. `printable()` (định nghĩa hàm nào "còn chọn được để in") nới theo cùng logic ở frontend. Vẫn CHẶN chọn dòng đang `queued` (đang gửi agent xử lý dở) — không cho gửi trùng lệnh khi lệnh cũ chưa xong. Reprint ghi đè `printed_at`/`queued_at` cũ (bảng không lưu lịch sử nhiều lần in cho cùng 1 UID, chỉ có đúng 1 dòng/1 trạng thái mới nhất) — đây là đánh đổi được chấp nhận, không phải lỗi.
- **`stagedItems` (Map, client-side) thay cho `stagedCodes` (Set) (26/09/2026, bản 4).** Đổi từ chỉ lưu mã UID sang lưu nguyên object dữ liệu (sku/productName/lot/roll/printStatus…) tại đúng thời điểm bấm "Đưa vào hàng chờ in" — để cụm Print vẫn hiển thị đúng tem dù tem đó đã "Đã in" (không nằm trong `cut_group_uid_list(p_status:'open')`, chỉ bảng Tra cứu mới có đủ mọi trạng thái). Có `mergeStaged()` chạy sau mỗi lần `loadOpen()`/`runSearch()` để cập nhật lại dữ liệu mới nhất (vd trạng thái chuyển pending→queued→printed) cho đúng các tem đang treo trong cụm Print, tránh hiển thị dữ liệu cũ. Bỏ tick 1 dòng trong cụm Print = xoá luôn khỏi `stagedItems` (rời khỏi "giỏ" in), không phải chỉ ẩn tạm; in thành công (`markResult` báo `printed`) cũng tự dọn khỏi `stagedItems` theo đúng `printJobId`.
- **Đưa thanh hành động lên đầu cụm Data/Print, tránh cuộn (26/09/2026, bản 5).** Trước đó "Xuất Excel"/"Đưa vào hàng chờ in" (cụm Data) và "Tải lại"/"In tem đã chọn" (cụm Print) nằm SAU danh sách/bảng — khi chọn nhiều dòng phải cuộn hết qua bảng mới bấm được. Giờ `.cut-actions` chuyển lên ngay sau `<h2>`/form lọc, TRƯỚC bảng/danh sách, nên bấm được ngay không cần cuộn dù danh sách dài bao nhiêu. Chỉ đổi thứ tự HTML thuần, không đổi id/JS nào cả.
- **Sửa lệch layout `.cut-actions` + đóng khung nút chuyển cụm (26/09/2026, bản 6).** `.cut-actions` trước chỉ `flex-wrap:wrap` trong media query mobile (`max-width:800px`) — ở các bề rộng KHÔNG rơi đúng vào 2 nhánh (mobile hẳn / desktop hẳn, ví dụ điện thoại lớn hoặc xoay ngang), nhãn "Chọn tất cả…" (`flex:1`, không có `flex-basis`) cùng 2 nút bên cạnh có thể bị bóp/lệch hàng vì không có chỗ để xuống dòng. Sửa: `.cut-actions{flex-wrap:wrap}` áp dụng LUÔN (không chỉ trong media query), và label đổi `flex:1` thành `flex:1 1 220px` để không bị bóp gần như biến mất trước khi tới ngưỡng xuống dòng. `.cut-link` (nút "Xem trong Tra cứu →" / "← Chọn thêm ở Tra cứu") trước là link chữ trần (`display:block`, không viền) — đổi thành nút bo tròn có viền giống `.cut-secondary` (đóng khung rõ ràng) theo đúng yêu cầu.
- **Đã khoá hẳn đường xóa (25/09/2026).** Phát hiện `cut_group_uids` mất 103/106 dòng từng insert (`pg_stat_user_tables.n_tup_del=103`), chưa xác định chắc chắn nguyên nhân. `cut_group_uid_remove` là đường xóa DUY NHẤT trong toàn bộ code, và nó từng được `grant execute` cho cả `anon` — nghĩa là bất kỳ ai mở trang web (app không yêu cầu đăng nhập) đều gọi được RPC này trực tiếp qua REST API, không nhất thiết phải qua nút bấm trên giao diện. Đã gỡ nút "×" + handler khỏi `index.html` và `revoke execute` khỏi `anon,authenticated` (`supabase/cut_group_uid_v2_lock_delete.sql`, không `drop function` — chỉ khóa quyền, để sau này có thể mở lại qua đường Admin có đăng nhập nếu cần, giống mô hình `group_uid_admin_allowed`). Đã xác nhận thật: gọi RPC bằng đúng publishable key của frontend trả về `401 permission denied for function cut_group_uid_remove`. Từ nay tem cắt nhầm không tự xóa được nữa — sai thì phải nhờ chỉnh trực tiếp trong database.
- **Chọn tất cả kiểu bảng WMS: checkbox nằm trong header thay vì nhãn riêng, tô sáng dòng đã chọn (26/09/2026, bản 7).** Theo tham khảo giao diện bảng WMS (trang Physical Count Result) người dùng gửi: ô "Chọn tất cả tem có thể in trong bảng" chuyển từ `<label>` đứng riêng phía trên bảng vào thẳng cột đầu tiên của `<thead>` (`.cut-th-check`), đúng vị trí/kiểu cột chọn của Ant Design Table. Thêm class `.is-selected` tô sáng nền dòng khi tick chọn — gắn/gỡ tự động ngay trong `syncSearchSelection()` (đã chạy sẵn ở mọi lần đổi tick) nên không cần thêm listener riêng cho từng dòng. Nút "Đưa vào hàng chờ in" giờ hiện luôn số lượng đang chọn ngay trên nút (vd "Đưa 233 tem vào hàng chờ in") thay vì chữ tĩnh, giống cách nút "In tem đã chọn" ở cụm Print đã làm. Chỉ đổi giao diện/CSS + cách gắn class chọn dòng, không đổi RPC hay logic chọn-đẩy-vào-hàng-chờ nào.

## Theo dõi Xả vải (26/09/2026)

- Object: một bản ghi `xa_vai_log`, khóa duy nhất là `group_uid_code` — số hoá đúng "PHIẾU THEO DÕI XẢ VẢI" giấy hiện có (cột STT, UID, Batch, Roll, SKU, Ngày bắt đầu xả, Giờ bắt đầu xả, Giờ xả vải xong). Không liên quan đến tính năng in tem `fabric_relaxation:v1/v2/v3` đã có từ trước (đó là loại tem in khác, cùng tên "xả vải" nhưng khác chức năng — dễ nhầm nên đặt tên bảng/route riêng `xa_vai_log`/`#xa-vai`, không dùng lại chữ `fabric`).
- Nguồn Field Value: `group_uid_details` cung cấp SKU/Batch/Roll — chụp lại lúc bấm "Bắt đầu xả vải" để lịch sử không đổi theo danh mục về sau, cùng nguyên tắc với `cut_group_uids`.
- Rule OK: UID phải có trong `group_uid_details` và có SKU mới cho bấm Bắt đầu. Bắt đầu chỉ chạy được khi UID đó CHƯA từng bắt đầu (khóa theo `group_uid_code`, một UID chỉ xả một lần); Kết thúc chỉ chạy được khi ĐÃ bắt đầu và CHƯA kết thúc.
- **Chỉ 1 UID được xả tại một thời điểm trên toàn hệ thống (chốt 26/09/2026).** `xa_vai_start` khoá bằng `pg_advisory_xact_lock` (giống `sample_scan`) rồi kiểm tra còn dòng nào `ended_at is null` không — có thì chặn kèm thông báo rõ mã UID đang xả, phải bấm Kết thúc UID đó trước. Tránh 2 điện thoại cùng bấm Bắt đầu gần như đồng thời cho 2 UID khác nhau.
- Trạng thái suy ra từ 2 cột thời gian, không lưu cột status riêng: chưa có dòng / có `started_at` chưa có `ended_at` (đang xả) / có cả hai (đã xong).
- Quét UID ở cụm 1 gộp làm một bước: submit form gọi thẳng `xa_vai_start` (tự tra `group_uid_details` bên trong RPC, không còn bước "Tra UID" tách riêng trước đó). Cụm 2 hiển thị UID đang xả (nhiều nhất 1 dòng do ràng buộc trên) kèm nút Kết thúc ngay trên dòng đó — không cần quét lại UID để kết thúc.
- Mốc thời gian dùng `now()` của Postgres (UTC), hiển thị luôn quy đổi tường minh theo `Intl.DateTimeFormat({timeZone:'Asia/Ho_Chi_Minh'})` ở frontend — không phụ thuộc múi giờ hệ điều hành của điện thoại thao tác.
- Ô quét (`#xa-vai-code`) dùng nguyên cơ chế đã chứng minh ở `#cut-code`: `inputmode="none"` ẩn bàn phím ảo kèm nút bật lại, và listener giữ focus toàn cục — xem chi tiết ở mục "Cắt Group UID" phía trên.
- Cụm 3 (Tra cứu/Đối chiếu, theo SKU, so khớp chứa, tối đa 5.000 dòng) gấp lại theo mặc định — chạm tiêu đề để mở/thu gọn, nhớ trạng thái theo `localStorage`, giống "Danh sách bao" ở màn `#sample`; chỉ gọi RPC tra cứu khi thật sự mở ra lần đầu. Xuất Excel A4 ngang đúng 8 cột như phiếu giấy, dùng chung `window.PrintSkuZip` đã có sẵn (không thêm thư viện ngoài).
- Backend bật RLS và thu hồi quyền bảng trực tiếp; frontend anon chỉ gọi RPC `xa_vai_start/xa_vai_end/xa_vai_list_open/xa_vai_search`, theo mô hình truy cập hiện hữu của ứng dụng (không đăng nhập).
- Nút quét bằng camera (`#xa-vai-camera`, 26/09/2026) **dùng chung engine dò mã của màn PRINT SKU** qua `window.PrintSkuScanner={gl,k0,ev,Jp}` — một dòng lộ hàm đã có sẵn ra ngoài bundle React (không viết lại logic, không thêm thư viện mới). Bản đầu tự viết riêng chỉ dựa `BarcodeDetector` gốc đã **không quét được trên iOS** (Safari/WebKit không có API này) — sửa bằng cách dùng lại `Jp()`, tự thử `BarcodeDetector` gốc trước rồi rơi về ZXing (đã bundle sẵn trong trang) nếu trình duyệt không có; đã xác nhận thật trên môi trường dev (cũng không có `BarcodeDetector` gốc): `Jp()` trả đúng nhánh `zxing`. Icon nút dùng đúng SVG "ScanLine" của Lucide như màn PRINT SKU (không phải emoji), lồng vào bên trong khung nhập (giống ô "Mã SKU" bên PRINT SKU) thay vì tách rời. Nút ẩn nếu không có `getUserMedia`/không phải secure context; nếu bấm mà cả hai đường dò mã đều không dùng được thì báo rõ trong khung camera thay vì treo im lặng. Quét được mã thì tự điền vào ô và tự submit form (gọi `xa_vai_start` ngay), giống hệt hành vi gõ tay/máy quét ngoài rồi Enter.
- Vẫn báo lỗi "IOS vẫn chưa nhận diện được mã vạch" sau khi đã dùng chung engine ở trên — đo thực tế trên điện thoại cho thấy `Jp()` chọn đúng nhánh `zxing`, camera mở đúng kích thước (video 720×1280), đã thử quét hàng trăm lần nhưng không ra kết quả dù mã vạch rõ trong khung hình. Nghi có mã UID khác/nhãn khác lọt vào khung hình đầy đủ gây rối ("quét lan"), và camera lấy nét/độ nét qua web có thể kém hơn app camera gốc — chưa loại trừ được khả năng đang test qua trình duyệt lồng bên trong một app chat nội bộ (webview) thay vì Safari thật, việc này có thể tự nó giới hạn camera bất kể sửa code gì.
- 26/09/2026 (bản 2): thêm **cắt vùng quét còn 50% chính giữa khung hình** trước khi đưa vào bộ đọc mã — mục tiêu giảm "quét lan" sang UID/nhãn khác nằm cạnh bên trong cùng khung hình. Có khung viền trắng kiểu spotlight (giống khung 5:2 bên PRINT SKU) làm gợi ý hình ảnh cho người dùng canh mã vào giữa. Vùng thật sự đưa vào bộ đọc dùng `canvas.captureStream()` tạo một video "ảo" chỉ chứa phần đã cắt (bộ đọc chung `Jp()` bắt buộc nhận `HTMLVideoElement` thật, không nhận canvas trực tiếp) — có tự kiểm tra: nếu máy/trình duyệt không hỗ trợ `captureStream` (hoặc không lấy được kích thước video ảo trong 800ms) thì tự động quay lại quét full khung hình như bản cũ, không làm hỏng tính năng hiện có. Ảnh đã cắt cũng hiển thị trực tiếp ngay trên màn hình camera (dưới dòng debug) để tự kiểm tra bằng mắt xem có bị đen/đứng hình không — chẩn đoán thêm cho trường hợp không có thiết bị iOS thật để debug trực tiếp.
- CHƯA VERIFY: quét camera thật trên điện thoại (đo thử chỉ giả lập được bằng mock `BarcodeDetector`/`getUserMedia` do môi trường dev không có camera thật) — cần thử trên điện thoại kho: xin quyền camera lần đầu, từ chối quyền, quét thành công, máy không có camera. Đối chiếu với phiếu giấy thật ngoài hiện trường. Đặc biệt cần xác nhận: (1) mở đúng bằng Safari thật (không phải trình duyệt lồng trong app chat) có quét được không, (2) sau khi cắt còn 50%, ảnh cắt hiển thị trên màn có rõ nét/đúng nội dung không hay bị đen (nếu đen tức `captureStream` không hoạt động trên máy đó), (3) có thật sự giảm được tình trạng quét lan sang UID khác không.
- **Chuyển 3 khung thành 3 cụm dạng tab: Scan/Đang xả/Đã xả xong (26/09/2026, bản 3).** Đồng bộ đúng cách làm vừa áp dụng cho màn Cắt Group UID: bỏ layout 3 khung hiện cùng lúc, giờ chỉ 1 cụm hiện nội dung tại một thời điểm (`#xa-vai-tab-scan`/`#xa-vai-tab-open`/`#xa-vai-tab-done`, nhớ theo `localStorage` khoá `xa-vai-cum-dang-mo`). Bỏ hẳn cơ chế gấp/mở lồng bên trong cụm 3 (`xa-vai-search-toggle`/`xa-vai-search-body`/`SEARCH_STORE` cũ) vì tab đã tự nhiên giải quyết đúng yêu cầu không hiển thị sẵn. Cụm 1 (Scan) có nút "Xem đang xả →" nhảy sang cụm 2. Thông báo trạng thái (`#xa-vai-message`) chuyển ra ngoài 3 panel, ngay dưới thanh tab, để thấy phản hồi dù đang ở cụm nào. Máy quét IME giữ focus ở ô quét giờ CHỈ áp dụng khi đang ở cụm Scan (`activeTab==='scan'`), tránh cướp focus khi đang ở cụm 2/3.
- **Cụm 2 "Đang xả" tải từ database, không phải state tạm trên trình duyệt (26/09/2026, bản 3).** Đây là hành vi đã có sẵn từ trước (RPC `xa_vai_list_open` đọc thẳng từ bảng `xa_vai_log where ended_at is null`, gọi lại mỗi khi vào màn qua `route()`) — chỉ xác nhận lại và đưa hẳn vào cụm riêng cho rõ: đóng ứng dụng giữa chừng khi đang xả một UID rồi mở lại (kể cả trên máy khác) vẫn thấy đúng UID đó ở đây để bấm Kết thúc, không mất dữ liệu. Đã kiểm tra thật: bắt đầu xả 1 UID thật (`[UID_DA_XOA]`, xoá lại sau khi test xong) → hiện đúng ở cụm 2 → bấm Kết thúc → chuyển đúng sang cụm 3.
- **Tự động làm mới cụm "Đang xả" theo định kỳ, không chỉ chờ bấm "Tải lại" (26/09/2026, bản 4).** Thêm `scheduleRefresh()`: cứ 15 giây gọi lại `xa_vai_list_open` một lần (kiểu `quiet`, không hiện thông báo "Đang tải danh sách…" làm phiền), theo cùng cơ chế `setTimeout` tự lặp lại như `pollJobs` bên Cắt Group UID. Bắt đầu khi vào màn (`route()` lúc `active()`), tự dừng hẳn khi rời màn (`clearTimeout` ở nhánh `else`) — không chạy ngầm khi người dùng đang ở màn khác. Lý do: trước đó chỉ có nút "Tải lại" bấm tay, nên nếu máy khác vừa bấm Kết thúc thì máy đang mở không tự biết cho tới khi ai đó tự bấm; giờ tự cập nhật trong lúc màn hình vẫn đang mở. Đã kiểm tra thật bằng cách theo dõi số lần gọi RPC: đúng 1 lần/~15 giây khi đang ở màn, 0 lần khi đã rời sang màn khác.
- **Sửa độ trễ cảm nhận được sau khi bấm Kết thúc (26/09/2026, bản 5).** Hai nguyên nhân: (1) nút "Kết thúc" KHÔNG hiện thông báo "đang xử lý" ngay khi bấm (khác với nút "Bắt đầu xả vải" đã có `tell("Đang bắt đầu xả vải…")` từ trước) — người dùng bấm xong không thấy gì cho tới khi mạng phản hồi xong, cảm giác như đứng hình; (2) phải đợi xong RPC `xa_vai_end` RỒI MỚI gọi tiếp `loadOpen()` để tải lại danh sách (`await` nối tiếp 2 vòng gọi mạng) thay vì cập nhật ngay. Sửa: thêm `tell("Đang ghi giờ kết thúc xả vải…")` ngay khi bấm; sau khi `xa_vai_end` báo thành công, TỰ xoá dòng đó khỏi `openItems` cục bộ và vẽ lại danh sách ngay lập tức (không đợi mạng lần 2), còn `loadOpen({quiet:true})` chạy nền không chờ (bỏ `await`) chỉ để đối chiếu lại với server. Đã kiểm tra thật: bấm Kết thúc → thông báo "Đang ghi giờ…" hiện ngay, danh sách về rỗng ngay khi có phản hồi thành công, không còn khoảng im lặng giữa chừng.
- **Nút Xuất Excel đổi thành icon (26/09/2026, bản 6).** Cả `#cut-export` (Cắt Group UID) và `#xa-vai-export` (Xả vải) đổi từ nút chữ đầy đủ "Xuất Excel A4 ngang" sang nút hình vuông chỉ có icon "Download" (SVG Lucide, `title`/`aria-label` giữ nguyên chữ đầy đủ cho a11y/tooltip), dùng class mới `.cut-icon-btn` (44×44, bo góc, cùng tông màu `.cut-secondary`) — tiết kiệm không gian hàng hành động, nhường chỗ cho nút chính (vd "Đưa vào hàng chờ in") vẫn giữ dạng chữ vì là hành động chính.
- **Lọc "Đã xả xong" đổi từ 1 ngày thành KHOẢNG ngày (26/09/2026, bản 6).** `xa_vai_search` đổi tham số `p_date` thành `p_date_from`/`p_date_to` (đổi chữ ký nên phải `drop function` rồi tạo lại, xem `supabase/xa_vai_log_v4_search_date_range.sql`) — lọc theo khoảng `>= p_date_from and <= p_date_to`, vẫn theo cột "Ngày bắt đầu xả" (giờ VN). Giao diện: 2 ô ngày nối bằng mũi tên "→" (`.cut-daterange`), để trống 1 trong 2 đầu vẫn lọc đúng (chỉ ràng buộc đầu có nhập). Đã kiểm tra thật: trong khoảng ra đúng, ngoài khoảng ra 0, chỉ nhập "từ ngày" (bỏ trống "đến ngày") vẫn ra đúng kết quả.
- **Sửa lỗi cụm "Đã xả xong" không tự cập nhật sau khi bấm Kết thúc, phải tải lại trang mới thấy (26/09/2026, bản 7).** Người dùng báo qua ảnh chụp điện thoại thật: bấm Kết thúc xong, thông báo xác nhận đúng, nhưng chuyển sang tab "Đã xả xong" vẫn thấy "Chưa có dữ liệu phù hợp" — chỉ hiện ra sau khi tải lại toàn trang. Nguyên nhân: handler nút "Kết thúc" chỉ gọi `loadOpen({quiet:true})` (làm mới cụm 2 "Đang xả") chứ chưa từng gọi `runSearch()` (dữ liệu cụm 3), nên `searchItems`/bảng "Đã xả xong" chỉ được nạp đúng một lần lúc mới vào màn (`route()`), không tự đồng bộ lại khi có UID vừa kết thúc trong lúc màn vẫn đang mở. Sửa: thêm tham số `{quiet=false}` cho `runSearch()` giống hệt kiểu `loadOpen()` đã có (`quiet:true` chỉ tắt thông báo "Tìm thấy N dòng" khi thành công, KHÔNG tắt thông báo lỗi) — gọi thêm `runSearch({quiet:true})` (không `await`, chạy nền) ngay sau `loadOpen({quiet:true})` trong handler Kết thúc, không đổi lời gọi `runSearch()` (không quiet) ở `route()` khi mới vào màn. Đã kiểm tra thật bằng UID `[UID_DA_XOA]` (xoá lại sau khi test xong): Bắt đầu → Kết thúc → chuyển ngay sang tab "Đã xả xong" KHÔNG tải lại trang → huy hiệu tăng đúng, bảng hiện đúng dòng mới, thông báo thành công "Đã ghi giờ kết thúc xả vải cho..." vẫn còn nguyên (không bị đè bởi lỗi).
- **Cụm 3 đổi tên "Tra cứu/Đối chiếu" → "Đã xả xong", thêm lọc theo ngày (26/09/2026, bản 3).** `xa_vai_search` đổi chữ ký: thêm `p_date date default null` (chèn giữa `p_sku` và `p_limit` nên phải `drop function` chữ ký cũ rồi tạo lại, xem `supabase/xa_vai_log_v3_search_date_filter.sql`). Hai thay đổi cùng lúc: (1) CHỈ trả về dòng đã `ended_at is not null` — UID đang xả dở đã có cụm 2 riêng, không cần lẫn vào đây nữa; (2) lọc thêm theo ngày bắt đầu xả (so khớp đúng ngày theo giờ Việt Nam `(started_at at time zone 'Asia/Ho_Chi_Minh')::date = p_date`, cùng cột "Ngày bắt đầu xả" đã hiển thị trên bảng) — không lọc theo ngày kết thúc vì đó không phải cột đang hiển thị, tránh gây hiểu lầm. Đã kiểm tra thật: lọc đúng ngày hôm nay ra 1 dòng, lọc ngày 2020-01-01 ra 0 dòng, không lọc ngày trả về đủ.

## WMS Design System (26/09/2026)

- Theo `docs/WMS_FRONTEND_UI_DIRECTION.md` — hướng cải thiện giao diện theo phong cách WMS, chia làm 3 giai đoạn để giảm rủi ro hồi quy trên app không có build step/source React gốc. Mục này chỉ ghi lại **Giai đoạn 1 — Chuẩn hóa nền tảng**, tự nó KHÔNG đổi giao diện màn nào.
- **Thêm design tokens + lớp CSS `.wms-*` dùng chung, chưa gắn vào màn nào (26/09/2026, bản 1).** Khối `<style id="wms-design-system">` mới, đặt ngay trước `</body>` (cuối `index.html`, sau toàn bộ script hiện có) — chỉ định nghĩa `:root{--wms-brand;--wms-brand-hover;--wms-bg;--wms-surface;--wms-text;--wms-muted;--wms-border;--wms-success;--wms-warning;--wms-danger}` và các lớp `.wms-header/.wms-card/.wms-input/.wms-button(--primary/--secondary/--danger)/.wms-tabs/.wms-tab/.wms-table*/.wms-status(--success/--warning/--danger)/.wms-action-bar` đúng theo mục 4 của tài liệu, cộng thêm `.wms-shell`/`.wms-grid` (khung bố cục max-width 1280px + grid 1 cột từ 375px) để Giai đoạn 2 có sẵn container dùng ngay. KHÔNG đổi JS/route/nghiệp vụ, KHÔNG sửa bất kỳ selector/class nào đang tồn tại — toàn bộ tên lớp `wms-*` xác nhận chưa trùng với bất kỳ class nào trong file trước khi thêm. Vì các lớp này chưa được gắn vào HTML của màn nào nên khối CSS này là bất hoạt (dead code có chủ đích) cho tới khi Giai đoạn 2 áp dụng từng màn một.
- **ĐÍNH CHÍNH (26/09/2026, sau khi soi màn PRINT SKU):** nhận định "body gốc app vốn đã tông sáng" ở trên SAI — bỏ sót rằng ngay sau `body{color:#1e293b;background-color:#f4f6f5}` (dòng ~331) còn có `html, body { min-height:100%; background:#07110e; }` (dòng ~339, cùng khối `<style>` đầu file) đè lên bằng đúng độ đặc hiệu + đứng sau nên thắng theo cascade — nền `<body>` THỰC TẾ đã resolve (đo bằng `getComputedStyle` khi đứng ở `#worker`) là tối `rgb(7,17,14)`, không phải sáng. PRINT SKU (React/Tailwind, mount ở `#root`) không tự phủ nền riêng (`getComputedStyle(root).backgroundColor` = trong suốt) nên đang ăn theo đúng nền tối này của `body`. Nghĩa là Giai đoạn 1/2 sang tông sáng là một THAY ĐỔI THẬT so với toàn bộ app hiện có (kể cả phần build sẵn), không phải "quay về nền gốc" như ghi nhầm ở trên — đã được người dùng xác nhận rõ ("Giai đoạn 1 Như tài liệu đề xuất") nên vẫn tiến hành, chỉ sửa lại cho đúng lý do.
- **Ràng buộc quan trọng cho Giai đoạn 2 sau này:** app có nhúng bundle React/ReactDOM đã biên dịch (xác nhận có banner license `react-dom.production.min.js` trong file) — nhiều khả năng chạy các màn gốc (PRINT SKU, có thể cả Inspection/Sample/Fabric Relaxation). Với các màn đó CHỈ nên chỉnh bằng CSS đè theo class/selector có sẵn, KHÔNG thể sửa cấu trúc DOM vì không còn source để build lại (đúng cảnh báo ở AGENTS.md). Các màn viết tay thuần JS (Cắt Group UID, Xả vải, Print UID) thì được tự do áp `.wms-*` và đổi cả cấu trúc HTML nếu cần.
- Đã kiểm tra: tải lại `#home` và `#xa-vai` sau khi thêm khối CSS này — giao diện các màn không đổi gì (đúng như kỳ vọng "bất hoạt"), không có lỗi console mới ngoài các lỗi mạng nền quen thuộc (Supabase/CDN không gọi được trong môi trường dev), `git diff --check` sạch.
- **Giai đoạn 2, màn thứ hai: PRINT SKU (`#worker`) (26/09/2026, bản 3).** Màn này chạy trên bundle React/Tailwind đã biên dịch (mount ở `#root`, không có source để sửa cấu trúc — đúng ràng buộc đã ghi ở trên), nhưng khi soi kỹ hoá ra toàn bộ giao diện tối của màn này chỉ đến từ MỘT khối CSS viết tay, dễ đọc: `<style id="hasaki-slate-theme">` (dòng ~337-476), gồm các rule đè `!important` theo đúng selector/class Tailwind đã render (`#root .bg-white`, `#root .text-slate-800`, `#root input`...). Bản thân bundle Tailwind vẫn giữ nguyên các token màu gốc y hệt tông WMS (`--color-brand:#005f41` trùng `--wms-brand`, class `bg-[#f4f6f5]/93` trên header và `bg-white/96` trên thanh hành động dưới cùng vốn đã đúng ý định "nền sáng" từ đầu, chỉ là bị khối override ở trên ghi đè thành tối). Vì vậy cách chỉnh AN TOÀN nhất là chỉ đổi **giá trị màu** trong đúng các rule `#root ...` sẵn có của khối này sang token `--wms-*` (nền/card/input → `var(--wms-surface)`/`#fff`; chữ → `var(--wms-text)`/`var(--wms-muted)`; viền → `var(--wms-border)`; `.text-brand`/nút `.bg-brand` giữ nguyên vì đã đúng `#005f41`/`#087b58`), bỏ hẳn rule `#root .bg-brand-light` (giá trị tối `rgba(0,95,65,.22)` không cần nữa vì token gốc `--color-brand-light:#e6f3ef` của bundle vốn đã đúng cho nền sáng), thêm `color-scheme:light` scoped riêng cho `#root` (vì `:root{color-scheme:dark}` ở khối global vẫn giữ nguyên, không đụng tới — ngoài phạm vi "chỉ 1 màn" của giai đoạn này). KHÔNG đổi 3 dòng global đầu khối (`:root{color-scheme:dark}`, `html,body{background:#07110e}`, `body{color:#e7f1ed}`) vì các màn viết tay khác (Trang chủ, Cắt Group UID, Xả vải...) đều tự phủ nền riêng full-bleed nên không bị ảnh hưởng, nhưng những màn overlay chưa rà (`admin-auth`, `admin-session`, `group-uid-import-screen`, `gui-update-modal`) chưa được xác minh — đổi global lúc này là vượt phạm vi. Đã kiểm tra thật trên Browser pane: nạp lại `#worker`, xác nhận nền/card/input/label/placeholder/badge trạng thái đều sáng và đọc được (không còn chữ sáng chìm trên nền sáng), test nhập liệu (SKU giả không có trong danh mục + để trống tên hàng) để xem đúng 2 banner cảnh báo màu cam vẫn tương phản tốt trên nền trắng, kiểm tra ở 3 kích thước (mặc định, 1280px desktop, 375px mobile) đều xếp lớp đúng không tràn ngang, nút back nổi (`barcode-home-trigger`) và điều hướng qua lại Trang chủ vẫn hoạt động, không có lỗi console mới ngoài baseline mạng quen thuộc. Không test được: máy quét/camera thật (bị chặn trong Browser pane), in tem thật (chưa có máy in), toàn bộ luồng nghiệp vụ Supabase thật (mạng bị chặn trong môi trường dev) — những phần này không đổi logic nên rủi ro thấp, nhưng cần người dùng tự xác nhận trên thiết bị thật trước khi coi là xong hẳn.
- **Chuẩn hoá font chủ đạo Arial toàn app (26/09/2026, bản 4).** Theo yêu cầu người dùng ("Font chủ đạo Arial ở giao diện fontend", khớp phong cách của WMS tham khảo). Trước đây phần lớn màn viết tay (Trang chủ, PRINT SKU/`#root`, admin-auth, sample-modal...) đã tự khai `font-family: Arial, Helvetica, sans-serif` riêng lẻ, nhưng `<body>` (dòng ~340, khối `<style id="hasaki-slate-theme">`) chưa khai font-family nào — nên bất kỳ phần tử nào không tự set riêng sẽ rơi về stack mặc định của bundle Tailwind (`--font-sans`: hệ `-apple-system/Segoe UI/Roboto...`) chứ không phải Arial. Thêm `font-family: Arial, Helvetica, sans-serif;` vào đúng rule `body{color:#e7f1ed}` đó để làm nền font mặc định cho toàn app (các màn đã tự khai Arial không đổi gì, chỉ là hết dư thừa/nhất quán). Riêng `.gui-screen` (màn Nhập Group UID, dòng ~2515) đang khai `font-family: Inter, Arial, sans-serif` — đổi thành `Arial, Helvetica, sans-serif` cho khớp, vì `Inter` chưa từng được nạp bằng webfont/Google Fonts nên trước giờ vẫn fallback về Arial trên thực tế, chỉ là khai báo sai gây hiểu nhầm. Thêm token `--wms-font: Arial, Helvetica, sans-serif;` vào khối `:root` của `<style id="wms-design-system">` và bổ sung dòng font vào mục 4 `docs/WMS_FRONTEND_UI_DIRECTION.md` để tài liệu khớp thực tế đã áp dụng. Đã kiểm tra thật trên Browser pane: `getComputedStyle(document.body).fontFamily` và `getComputedStyle(#root).fontFamily` đều trả về `"Arial, Helvetica, sans-serif"`, `.gui-screen` cũng vậy, PRINT SKU vẫn hiển thị đúng sau khi đổi, không có lỗi console mới, `git diff --check` sạch.
- **Giai đoạn 2, màn thứ ba: CẮT GROUP UID (`#cut-group-uid`) (26/09/2026, bản 5).** Màn này là JS/CSS/HTML viết tay thuần (không phải bundle biên dịch) nên được tự do hơn, nhưng vẫn chọn cách chỉnh AN TOÀN giống PRINT SKU: chỉ đổi **giá trị màu** trong đúng các rule `.cut-*` sẵn có ở `<style id="cut-group-uid-theme">` sang token `--wms-*`, KHÔNG đổi cấu trúc HTML/JS/route/nghiệp vụ. Header sticky chuyển nền trắng mờ + viền sáng (giữ nguyên `position:sticky`); 3 card (Quét/Tra cứu/Tem chờ in) chuyển nền trắng + viền sáng + thêm bóng đổ nhẹ; tab chuyển nền trắng/viền sáng, tab đang chọn dùng tông xanh brand nhạt (`rgba(0,95,65,.08)`) + chữ brand thay vì nền xanh đậm/chữ trắng cũ; badge số lượng, trạng thái tem (`.cut-status` — pending/failed/queued), banner thông báo (ok/warn/error) đều đổi sang cặp nền nhạt + chữ đậm cùng tông (`--wms-success/--wms-warning/--wms-danger`) thay vì chữ sáng trên nền tối; input/bảng/nút phụ (secondary/icon/link) đổi sang nền trắng + viền `--wms-border` + chữ `--wms-brand` hoặc `--wms-muted` tuỳ vai trò. Bắt lỗi tương phản ẩn: `.cut-card h2 span` (số thứ tự 1/2/3 trong vòng tròn xanh) trước đây không khai `color` riêng nên ăn theo màu chữ sáng của toàn màn — khi đổi sang nền sáng sẽ thành chữ tối trên nền xanh đậm (mất tương phản), đã thêm `color:#fff` tường minh để giữ đúng như cũ. **Đồng thời hoàn thiện luôn phần chọn tất cả ở "Tem chờ in" đã tạm dừng dở từ trước** (đúng hướng WMS, xem mục Cắt Group UID phía trên): `renderOpen()` nay in thêm `data-selected="${printable(item)}"` vào mỗi `.cut-row` (điều khiển highlight viền/nền xanh nhạt qua rule `.cut-row[data-selected="true"]` đã có sẵn), `syncSelection()` nay cập nhật `selectedCountEl.textContent` thành `Đã chọn N` mỗi khi tick/bỏ tick. Đã kiểm tra thật trên Browser pane với dữ liệu Supabase thật (233 UID đã cắt, SKU [SKU_DA_XOA]): cả 3 cụm hiển thị đúng màu sáng và đọc được, chọn hết ở bảng Tra cứu → bấm "Đưa vào hàng chờ in" → sang cụm Tem chờ in thấy đúng "Đã chọn 233", bỏ chọn 1 dòng thấy dòng biến mất khỏi danh sách và đếm còn "Đã chọn 232" (đúng thiết kế: bỏ tick = xoá khỏi hàng chờ, không phải ẩn tạm), kiểm tra mobile 375px xếp đúng không tràn ngang, không có lỗi console mới. CHỈ thao tác trên state client (`stagedItems`, chưa gọi RPC `cut_group_uid_mark_queued`/enqueue in) — không bấm nút "In tem" thật để tránh tạo lệnh in/ghi dữ liệu thật vào Supabase production, nên chưa test được toàn bộ luồng in thật (giữ nguyên rủi ro thấp vì không đổi logic in). `git diff --check` sạch.
- **Giai đoạn 2, màn thứ tư: PRINT UID (`#group-uid`, khối `.uid-*`) (26/09/2026, bản 6).** Cùng cách làm "chỉ đổi giá trị màu, giữ nguyên selector/HTML/JS" như PRINT SKU và CẮT GROUP UID. Topbar sticky, card, tab gán SKU (tick chọn/tự động), khung "Chưa gán SKU"/"Sẵn sàng in", pill chọn tất cả, badge trạng thái máy in (`data-tone="ok"/"warn"`), ô nhập, khung gán tự động theo số lượng — tất cả chuyển từ nền tối sang token `--wms-*`. Giữ nguyên không đổi các nút solid màu thương hiệu vốn đã tự đủ tương phản trên mọi nền (`.uid-topbar__mark`, `.uid-add__btn`, `.uid-tabs button[aria-selected="true"]`, `.uid-map__apply`, `.uid-submit`, `.uid-badge--ready`). Đã kiểm tra thật trên Browser pane ở `#group-uid`: đủ 4 cụm (Thêm Group UID/Gán SKU/Chưa gán SKU/Sẵn sàng in) hiển thị sáng và đọc được, badge trạng thái máy in "Chưa rõ máy in" (tông warning) vẫn nổi rõ, layout mobile 375px không tràn ngang (`scrollWidth===innerWidth`), không có lỗi console mới, `git diff --check` sạch.
- **Giai đoạn 2, màn thứ năm: THEO DÕI XẢ VẢI (`#xa-vai`) (26/09/2026, bản 7) — không cần sửa gì.** Màn này dùng lại y nguyên `class="cut-screen"` + toàn bộ `.cut-*` (header, tab, card, input, bảng, badge) — không có lớp CSS `.xa-*` riêng, không style inline nào trong JS render. Vì `.cut-*` đã được chuyển sang token `--wms-*` khi làm CẮT GROUP UID ở trên, màn này tự động ăn theo giao diện sáng, không cần đổi dòng nào. Đã kiểm tra thật trên Browser pane: cả 3 cụm (Quét/Đang xả/Đã xả xong, gồm cả bảng lọc theo khoảng ngày) hiển thị đúng sáng, không có lỗi console mới.
- **Giai đoạn 2, màn thứ sáu (cuối cùng): INSPECTION, SAMPLE, FABRIC RELAXATION (26/09/2026, bản 8) — hoàn tất toàn bộ Giai đoạn 2.** Cả 3 màn đều dùng chung `class="uid-screen"` + `.uid-topbar`/`.uid-card` làm khung (đã sáng sẵn từ khi làm PRINT UID), chỉ còn phần CSS riêng của từng màn cần đổi màu:
  - `.ins-*` (INSPECTION — bảng nhập liệu sau khi import PO): input/label/table-head/readonly chuyển sang token `--wms-*`, ô `readonly` (giá trị hệ thống tự điền) dùng `var(--wms-brand)` cho dễ phân biệt với ô người tự gõ.
  - `.sample-*` (SAMPLE — khung kết quả quét, danh sách bao, modal xác nhận bao mới): `.sample-result`/`.sample-row` đổi nền/viền/chữ sang token, tông "ok"/"error" đổi sang cặp nhạt-đậm cùng họ `--wms-success/--wms-danger`; `.sample-modal__card` (hộp thoại nổi giữa màn, nền mờ tối phía sau giữ nguyên vì là lớp che chuẩn, không phụ thuộc theme trang) đổi từ thẻ tối sang thẻ trắng, số thứ tự bao to màu brand.
  - `.fabric-*` (FABRIC RELAXATION — form số tem in + xem trước tem): card/input đổi sang token; `.fabric-preview` (khối SVG mô phỏng tem giấy thật) giữ nguyên nền trắng vì vốn đã trắng từ đầu (mô phỏng đúng giấy in). Phát hiện 8 rule `.fabric-code-*`/`.fabric-add-code` là CSS chết (không còn HTML/JS nào dùng, chắc còn sót từ bản thiết kế cũ có danh sách nhiều mã) — không đổi màu, để nguyên vì không ảnh hưởng và tránh mở rộng phạm vi ngoài yêu cầu.
  Đã kiểm tra thật trên Browser pane: INSPECTION (chèn thử 1 dòng `.ins-row` bằng JS để xem vì chưa có file PO để import), SAMPLE (dữ liệu thật 142 bao/245 mẫu, mở rộng danh sách xem từng dòng, bật thử modal "Bao mới" bằng JS không qua RPC thật), FABRIC RELAXATION (điều hướng bằng `#fabric-relaxation`, đúng hash JS dùng chứ không phải `#fabric`) — cả 3 đều sáng, đọc được, không lỗi console mới, mobile 375px không tràn ngang. **Đến đây, toàn bộ 6 mục Giai đoạn 2 theo `docs/WMS_FRONTEND_UI_DIRECTION.md` đã hoàn tất**, còn lại Giai đoạn 3 (đồng bộ/dọn biến thể CSS thừa toàn app) chưa bắt đầu.
- **Giai đoạn 2, màn đầu tiên: Trang chủ (`#barcode-home`) (26/09/2026, bản 2).** Đổi từ nền tối full-bleed (hero canh giữa, 1 lưới phẳng 7 nút ngang hàng) sang đúng hướng tài liệu: header xanh đậm (logo + "WH-MATERIAL" + mô tả) phía trên, vùng nội dung nền sáng (`var(--wms-bg)`) phía dưới, gom 7 chức năng thành 3 nhóm theo nghiệp vụ — **In tem** (PRINT SKU, PRINT UID, FABRIC RELAXATION), **Vận hành kho** (CẮT GROUP UID, THEO DÕI XẢ VẢI), **Kiểm tra** (INSPECTION, SAMPLE) — mỗi nhóm có tiêu đề nhỏ, các thẻ dùng `.wms-*` tokens (`var(--wms-surface)`/`var(--wms-border)`/`var(--wms-brand)`...) cho card trắng bo góc 14px + icon SVG riêng cho từng chức năng (Lucide-style, thay cho ký tự Unicode cũ vốn không hiển thị thật vì `color:transparent;font-size:0` — chỉ để lại icon trang trí giả bằng `::before`, đã bỏ luôn phần chết đó). Lưới thẻ đổi từ `grid-template-columns:repeat(4,...)` cố định sang `repeat(auto-fit,minmax(240px,1fr))` nên không cần media query riêng cho tablet nữa (đã xoá breakpoint `1080px/621px` cũ). CHỈ đổi HTML/CSS thuần: giữ nguyên toàn bộ 7 DOM ID nút (`barcode-print-sku`, `barcode-print-uid`, `barcode-cut-uid`, `barcode-inspection`, `barcode-sample`, `barcode-fabric`, `barcode-xa-vai`) và `barcode-home-trigger` (nút "← WH-MATERIAL" nổi dùng chung cho màn PRINT SKU chạy trên React bundle) y nguyên vì toàn bộ JS chỉ tra cứu qua `getElementById`, không phụ thuộc thứ tự/lồng cấp DOM — xác nhận không có nơi nào khác trong file dùng lại các class `barcode-home__*`/`barcode-choice*` trước khi sửa. Đã kiểm tra thật trên Browser pane: cả 7 nút điều hướng đúng hash (thử trực tiếp `barcode-cut-uid`→`#cut-group-uid`, `barcode-print-sku`→`#worker` là màn React), nút back nổi vẫn hiện đúng trên màn PRINT SKU, bố cục mobile 375px xếp 1 cột không bị tràn ngang (`document.documentElement.scrollWidth === innerWidth`), không có lỗi console mới, `git diff --check` sạch.
